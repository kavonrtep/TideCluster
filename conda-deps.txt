# Conda runtime dependencies, conda-format pins (single `=` not `==`).
# Consumed by `mamba install --file` in CI and by the Singularity
# container's %post block. Filename is intentionally not
# `requirements.txt` so GitHub Dependabot does not mistake it for a
# pip requirements file (it cannot parse conda-format pins).
TideHunter=1.4.3
blast
mmseqs2
python>=3.6
networkx
repeatmasker=4.1.2.p1
r-optparse
# Pinned: igraph drives Leiden community-detection tie-breaking in the
# comparative analysis; an unpinned version is a cross-environment
# reproducibility risk (see issue #4, R6).
r-igraph=2.0.3
bioconductor-biostrings
bioconductor-rtracklayer
bioconductor-genomicranges
bioconductor-genomeinfodbdata
r-scales
r-hwriter
r-r2html
r-plyr
r-dplyr
r-jsonlite
r-reshape2
mafft
bedtools
# Requires `-c petrnovak` in addition to conda-forge + bioconda.
kitehor=0.13.2
